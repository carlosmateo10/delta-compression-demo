services:
  - type: web
    name: delta-compression-demo
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: cd server && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
